class ListNode():
    def __init__(self, value, nextVal = None, before= None):   # next and before params are objects
        self.value = value
        self.next = nextVal
        self.before = before
    
